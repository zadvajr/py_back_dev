#Python - Random Number
#Python does not have a random() function but has a module called random for generating a random number

import random

print(random.randrange(1, 10)) #prints random integer between 1 and 9 each time the program is run

#end
