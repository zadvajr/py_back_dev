#Python lambda function is a small anonymous functions
#A lambda function can take any number of arguments but can only have a single expression
#syntax - lambda arguments : expression
# to convert that our sum() function to lambda function we do the following
sum = lambda a,b:  a + b

print(sum(5,10))