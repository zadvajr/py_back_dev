#Removing whitespace in a string

txt = "                this text has plenty white spaces before and after the actual text           "
print("before removing whitespace with strip(): ", txt)
print("after removing whitespace with strip(): ", txt.strip())

#end
