"""Using pop() to remove set items"""
# You can also use pop() to remove set items, but this removes a random item, and the return value of the pop is the removed set item
thisset = {"apple", "banana", "cherry"}

x = thisset.pop()

print(x)

print(thisset)