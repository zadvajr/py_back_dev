#Replace string
#this script will replace all white space with hyphen -

txt = "       this text is preceeded with whitespace and whitespace after too         the whit spaces including the one in between words will be replaced with hyphen"
print(txt.replace(" ", "-"))

#end
